<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.samsungnormalannualproject">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SamsungNormalAnnualProject"
        android:usesCleartextTraffic="true">
        android:theme="@android:style/Theme.NoDisplay"
        <activity android:name=".UserDataActivity"></activity>
        <activity android:name=".ViewingNominationsActvity" />
        <activity android:name=".WhatKindOfPeopleLookinFor" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".SignUpForm" />
        <activity android:name=".LoginOrSignUp" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SelectGenderActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>